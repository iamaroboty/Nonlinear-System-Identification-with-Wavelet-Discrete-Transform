% Testing Volterra filter
clear all;
close all;

addpath "Fast Volterra Filtering";

fs = 1024;
N = 2*fs;
freq = 100;
t = 0:1/fs:1-dt;
u = 1*sin(2*pi*freq*t);

figure; 
subplot(2, 2, 1);
plot(t,u);

U = fft(u, N);
